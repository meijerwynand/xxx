[general]
static=yes
writeprotect=no

[from-sip]
exten => _X.,1,NoOp(Incoming call from SIP provider)
same => n,Dial(SIP/${EXTEN}@sip-provider)
same => n,Hangup()

[outgoing]
exten => _1NXXNXXXXXX,1,NoOp(Outgoing call to ${EXTEN})
same => n,Dial(SIP/${EXTEN}@sip-provider)
same => n,Hangup()